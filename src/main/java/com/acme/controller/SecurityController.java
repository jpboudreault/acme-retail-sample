package com.acme.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("api/")
public class SecurityController {
    

//    https://example.com/login/openid={openid}&accountId={accountIdentifier}
//
//    @RequestMapping("/api/products/{productId}/monthly-sales")
//    public Iterable<MonthlySale> products(@PathVariable long productId) {
//        Product product = productRepository.findOne(productId);
//        if (product == null) {
//            throw new ResourceNotFoundException();
//        }
//
//        return monthlySaleRepository.findByProduct(product);
//    }}
}